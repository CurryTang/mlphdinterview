import os

zh_content = """# 工业级推荐排序全景：模型体系选型、样本采样策略与多层级指标评估体系

在工业级推荐系统（Industrial Recommender Systems）与计算广告工程中，构建一个高吞吐、高精度、多目标协同的排序（Ranking）体系是核心技术壁垒。面对面试官或技术架构评审时，**如何系统性、结构化地阐述排序基座模型选型（Base Model Family）、训练样本规模与负采样方案（Sampling Scheme），以及多层级离线与线上指标评估体系（Ranking vs. Business Metrics, Calibration, Slices）**，是区分初级工程师与资深算法专家的关键分水岭。

本篇系统梳理工业级推荐排序全链路四大核心支柱：
1. **排序基座模型家族架构（Base Model Family: 特征交叉、序列建模与多任务学习）**
2. **训练样本规模构建、负采样范式与概率校准数学推导（Sampling Scheme & Probability Recovery）**
3. **金字塔指标评估体系（排序指标 vs 业务指标、校准曲线 PCOC、细分人群分片切片）**
4. **工业界经典高频面试题标准答题模板与方法论**

---

## 模块一：推荐排序基座模型家族（Base Model Family）

工业级推荐排序模型通常处理**百亿级高维稀疏特征（ID Features）**与**稠密数值特征（Dense Features）**，其演进脉络聚焦于三大核心技术轴线：

```text
工业级推荐精排模型演进三维谱系：
┌────────────────────────────────────────────────────────────────────────┐
│ 1. 显式与隐式特征交叉 (Feature Interaction)                            │
│ • 浅层线性 ➔ FM / FFM ➔ Wide&Deep ➔ DeepFM ➔ DCN-v2 (低秩张量交叉)    │
│ • 核心能力: 捕捉高阶非线性特征组合，兼顾泛化记忆与特征组合能力        │
└───────────────────────────────────┬────────────────────────────────────┘
                                    ▼
┌────────────────────────────────────────────────────────────────────────┐
│ 2. 用户长短期行为序列建模 (User Behavior Modeling)                     │
│ • Pooling (Sum/Mean) ➔ DIN (目标注意力 Target Attention) ➔             │
│ • SIM (两阶段超长序列检索: 硬检索 Hard-search + 软检索 Soft-search)    │
│ • 核心能力: 从用户数百乃至数万条历史交互序列中，动态提取与候选物关联的兴趣│
└───────────────────────────────────┬────────────────────────────────────┘
                                    ▼
┌────────────────────────────────────────────────────────────────────────┐
│ 3. 多目标联合学习与负荷路由 (Multi-Task Learning, MTL)                 │
│ • Shared-Bottom ➔ MMoE (多门控混合专家) ➔ PLE (渐进式分层抽取路由)    │
│ • 核心能力: 解决多任务之间的梯度冲突与负迁移 (Negative Transfer)       │
└────────────────────────────────────────────────────────────────────────┘
```

### 1. 经典工业级基座模型三大支柱

1. **特征交叉基座（Feature Interaction Backbone）**：
   - **DCN-v2 (Deep & Cross Network v2)**：通过显式多项式交叉层（Cross Network）结合低秩矩阵分解（Low-Rank MoE Matrix Multiplication），在极低延迟下逼近任意高阶特征交叉；
   - **DLRM (Deep Learning Recommendation Model)**：Meta 开源的标准架构，将特征明确解耦为用于稀疏 ID 的 Embedding 表与用于数值特征的 Bottom MLP，通过显式 Dot-product 交互后喂入 Top MLP。
2. **行为序列建模基座（User Behavior Sequence Backbone）**：
   - **DIN (Deep Interest Network)**：利用候选 Item 与历史行为序列计算 **Target Attention**，克服了传统 Pooling 对用户多样化兴趣的静态压缩损失；
   - **SIM (Search-based Interest Model)**：将用户行为序列扩展到数万步（Long-Term Sequence），第一阶段使用类目/Tag 倒排索引做 **Sub-sequence 快速检索（Hard Search）**，第二阶段在小集合上执行精细 Attention（Soft Search）。
3. **多目标排序基座（Multi-Task Learning Backbone）**：
   - **MMoE (Multi-gate Mixture-of-Experts)**：为每个任务分配独立的 Softmax 门控网络，动态加权共享 Expert；
   - **PLE (Progressive Layered Extraction)**：将专家网络严格区分为 **任务独占专家（Task-Specific Experts）** 与 **全局共享专家（Shared Experts）**，彻底阻断了点击（CTR）与转化（CVR）等相关性较弱任务间的负迁移现象。

---

## 模块二：训练样本规模、负采样方案与概率还原数学推导

在工业界，精排模型（Ranking）与召回模型（Retrieval）在样本构建和采样范式上存在本质区别：

```text
召回 (Retrieval) vs 精排 (Ranking) 样本构建本质差异：
┌───────────────────────┬────────────────────────────────────────────────────────────────────────┐
│ 环节                  │ 样本池来源与负样本采样策略                                             │
├───────────────────────┼────────────────────────────────────────────────────────────────────────┤
│ 召回阶段 (Retrieval)  │ • 候选空间: 全库数亿级候选 Item。                                      │
│                       │ • 负样本机制: 全库随机负采样 (Random Negatives) + 曝光未点击弱负例     │
│                       │   + 粗排截断硬负例 (Hard Negatives) + Cross-Batch In-batch Negatives。 │
├───────────────────────┼────────────────────────────────────────────────────────────────────────┤
│ 精排阶段 (Ranking)    │ • 候选空间: 经前序召回与粗排筛选后的 Top 1000~3000 候选集。            │
│                       │ • 正样本: 用户真实产生交互的行为（如点击、点赞、购买、完播）。         │
│                       │ • 负样本: 真正展现给用户但用户未产生交互的曝光（Impression-Unclicked）。│
│                       │ • 严禁机制: 精排严禁随意引入全库随机未曝光负样本（否则破坏真实曝光分布）│
└───────────────────────┴────────────────────────────────────────────────────────────────────────┘
```

### 1. 训练样本规模（Sample Size & Streaming Pipeline）

- **滚动时间窗口（Rolling Horizon）**：通常采用近 **14 天 ~ 30 天** 的全量曝光与点击日志（规模常达 **数十亿至数百亿条样本**）；
- **样本新鲜度与在线流式更新（Online Streaming Update）**：
  - 离线 Batch 训练更新天级基座大模型（Daily Checkpoints）；
  - 线上准实时流式计算管道（Flink/Kafka）在数分钟内拼接曝光与点击日志，对模型权重与 Embedding 表执行流式增量微调（Streaming Hourly / Real-time Updates），捕捉突发热点。

---

### 2. 负样本下采样（Negative Downsampling）与概率还原数学推导

#### 为什么工业级精排必须做负样本下采样？
在信息流或电商推荐中，真实点击率通常极低（如 $p \approx 1\% \sim 5\%$），正负样本极度不平衡（$1:20 \sim 1:100$）。
1. **节省训练计算资源与存储**：下采样负样本可将训练集体积缩减 70%~90%，大幅提高梯度迭代吞吐量；
2. **防止梯度被海量易分负样本淹没**。

#### 下采样率对预估概率的系统性扭曲
假设我们将未点击负样本以采样率 $w \in (0, 1]$ 进行随机下采样（保留比例为 $w$），而正样本全部保留。
下采样后，模型学到的条件概率 $\hat{p} = P(Y=1 \mid X, \text{sampled})$ 会被**人为大幅拉高**：

$$\hat{p} = \frac{P(Y=1 \mid X)}{P(Y=1 \mid X) + w \cdot P(Y=0 \mid X)} = \frac{p}{p + w(1-p)}$$

#### 线上推理时的概率校准还原公式（Probability Recovery Formula）
在竞价广告（$eCPM = pCTR \times pCVR \times \text{Bid}$）或需要精准概率融合的业务中，必须在模型输出后通过解析反函数将 $\hat{p}$ **精确还原为真实自然世界概率 $p$**：

$$p = \frac{\hat{p}}{\hat{p} + \frac{1 - \hat{p}}{w}}$$

```text
概率还原验证示例：
若真实 CTR p = 0.01 (1%)，负样本下采样保留率 w = 0.1 (10%):
1. 训练集上模型观测到的 CTR: p_hat = 0.01 / (0.01 + 0.1 * 0.99) = 0.01 / 0.109 = 0.0917 (9.17%)
2. 线上推理时通过公式还原: p = 0.0917 / (0.0917 + (1 - 0.0917) / 0.1) = 0.0917 / (0.0917 + 9.083) = 0.01 (精准复原为 1%!)
```

---

## 模块三：多维度指标评估金字塔（The Metric Pyramid）

```text
推荐系统多层级指标评估金字塔：
                     ▲
                    / \     【顶层：商业与北极星业务指标 Business Metrics】
                   /   \    • DAU / MAU, 人均时长, 购买 GMV, D7/D30 留存, 作者生态多样性
                  /─────\
                 /       \   【中层：分片与公平性指标 Slice & Guardrail Metrics】
                /         \  • 新老用户分群, 冷启动 Item, 细分类目切片, P99 耗时
               /───────────\
              /             \ 【底层：算法与排序离线指标 Ranking & Offline Metrics】
             /               \• AUC, GAUC (按 User 分组), LogLoss, PCOC 校准曲线, NDCG@K
            └─────────────────┘
```

### 1. 离线排序指标 vs. 线上业务指标的本质解耦

| 指标层级 | 核心指标 | 计算机制与数学定义 | 优缺点与工业界洞察 |
|---|---|---|---|
| **全局离线排序** | **Global AUC** | 计算全量测试集中正样本预测得分高于负样本的概率。 | **易受用户活跃度偏倚扭曲**：如果模型只学到了“给高活用户打高分、给低活用户打低分”，Global AUC 很高，但对单个用户内部的排序一塌糊涂。 |
| **个性化精细排序** | **Group AUC (GAUC)** | 在每个用户 $u$ 内部单独计算 $\text{AUC}_u$，然后按曝光量或点击量加权平均：<br>$$\text{GAUC} = \frac{\sum_{u} w_u \cdot \text{AUC}_u}{\sum_u w_u}$$ | **工业界精排离线核心北极星**！剥离了用户间静态偏置，纯粹衡量模型在“单个用户展示列表内部的相对排序优劣”。 |
| **概率打分质量** | **LogLoss (交叉熵)** | 衡量预测连续概率与真实 0/1 标签的对数损失。 | 直接反映概率拟合质量；对绝对数值极其敏感，受负采样影响大。 |
| **位置敏感度** | **NDCG@K / MRR** | 针对 Top-K 推荐列表，引入对数位置衰减因子 $\frac{1}{\log_2(\text{rank}+1)}$。 | 贴合用户视觉只看首屏（Top-3 / Top-5）的真实浏览习惯。 |
| **线上业务北极星** | **CTR / CVR / GMV / Retention** | 真实线上 A/B 测试中用户的实际转化与生态留存。 | 终极业务目标，包含网络效应与长周期反馈。 |

---

### 2. 校准度评估：PCOC 与可靠性曲线（Calibration & PCOC）

在计算广告（$eCPM$ 竞价排序）与混合目标融分中，**仅有高 AUC（排序对）是远远不够的，预估概率的绝对值必须精准反映真实物理概率**！

#### PCOC（Predictive-over-Observed Calibration Ratio）
$$\text{PCOC} = \frac{\sum_{i=1}^N \hat{p}_i}{\sum_{i=1}^N y_i} = \frac{\text{模型全样本预估平均概率}}{\text{真实观测平均点击率}}$$

- **$\text{PCOC} = 1.00$**：完美校准；
- **$\text{PCOC} > 1.00$**：模型整体**高估（Over-estimation）**（可能导致广告主预算过快消耗、竞价出价畸高）；
- **$\text{PCOC} < 1.00$**：模型整体**低估（Under-estimation）**（导致优质广告曝光不足、平台变现效率受损）。

#### 可靠性曲线（Reliability Diagram / Calibration Plots）
将预测概率分装到 10~20 个等频分桶（Bins）中，横轴绘制每个桶的平均预估概率 $\bar{\hat{p}}$，纵轴绘制该桶的实际经验转化率 $\bar{y}$。若曲线严格贴合 $y = x$ 45度对角线，则说明在不同置信度区间均具备优良校准性。

```text
校准曲线形态示意 (Reliability Plot):
实际观测 CTR (Observed)
  1.0│                                    . (完美对角线 y=x)
     │                                 . 
     │                             .  / (模型在高分段低估)
     │                         .     /
     │                     .        /
     │                 .  ──────────  (模型在中分段平坦失真)
     │             .  /
     │         .     /
     │     .        / (模型在低分段高估)
  0.0└─────.───────────────────────────► 预估 CTR (Predicted)
    0.0                                1.0
```

---

### 3. 分片指标与异质性分析（Slice Metrics）

大盘总体 AUC 增加 $0.5\%$ 并不代表所有用户都在获益。在工业界，必须执行**多维度切片分析（Subgroup / Slice Analysis）**：

1. **用户生命周期切片**：新注册用户（Cold-start）vs 低活跃用户 vs 高活跃重度用户；
2. **内容/商品切片**：头部爆款内容 vs 腰尾部长尾冷启动内容（Long-tail Items）；
3. **技术与客户端切片**：Android vs iOS，WiFi vs 弱网环境，不同地理区域（Geo-regions）。

> ⚠️ **防范辛普森悖论（Simpson's Paradox）**：
> 有时大盘 AUC 上涨，但由于高活用户样本量占比过大掩盖了低活用户的剧烈退化。切片分析能及时揪出特定重要子群的体验损伤。

---

## 模块四：面试标准实战答题架构（How to Answer This Question）

> 💡 **面试高分回答框架**：
> 当面试官提问：“请介绍你负责的推荐排序链路，从基座模型、样本与采样、到你看的各项指标体系，以及采样如何影响指标解读？”你可以按以下标准 4 步走进行降维打击：

```text
推荐排序体系全景答题四步法:
┌────────────────────────────────────────────────────────────────────────┐
│ 步骤一：基座模型选型 (Base Model Family)                               │
│ • 阐明架构解耦: Embedding + DCN-v2/DLRM 交叉 + DIN/SIM 序列 + PLE 多任务│
├────────────────────────────────────────────────────────────────────────┤
│ 步骤二：样本流与采样策略 (Sample Size & Sampling Strategy)             │
│ • 数据规模: 滚动 14~30 天数十亿级曝光日志 + Flink 流式实时微调          │
│ • 采样哲学: 精排严格使用曝光未点击作为真实负例；负采样需做重要性还原   │
├────────────────────────────────────────────────────────────────────────┤
│ 步骤三：指标金字塔体系 (The Multi-Tier Metric Stack)                   │
│ • 离线排序 (GAUC/NDCG) ➔ 概率校准 (PCOC/Reliability) ➔ 线上业务 (CTR/GMV)│
├────────────────────────────────────────────────────────────────────────┤
│ 步骤四：采样与规模对指标意义的底层影响 (Why Sampling Shifts Metrics)   │
│ • 深度推导负采样对 LogLoss 与基线 CTR 的扭曲，展示概率还原数学公式     │
└────────────────────────────────────────────────────────────────────────┘
```

### 1. 结构化陈述示范（Verbatim Reference）

1. **模型家族（Base Model）**：
   “我们在精排层采用分层解耦的工业级架构：底层使用 Embedding 表表征稀疏特征，特征交互层采用 **DCN-v2 与 DLRM 点积** 结合以捕获非线性显式交叉；针对用户中长周期兴趣，采用 **SIM 两阶段检索（Hard Search + Soft Attention）** 提取长序列特征；上层采用 **PLE（Progressive Layered Extraction）** 多任务网络同时联合建模点击率（pCTR）、长读率（pLongView）与互动率（pInteract），有效消除负迁移。”
2. **样本规模与采样（Sample Size & Sampling）**：
   “训练集基于近 30 天的百亿级真实用户曝光日志构建，并配合线上小时级流式增量微调。在样本构建上，**精排与召回存在本质差异**：精排只采用真实的**曝光未点击（Impression-Unclicked）作为负样本**。针对极端正负不平衡，我们采用负样本随机下采样（保留率 $w = 10\%$），并在推理时通过校准公式 $p = \frac{\hat{p}}{\hat{p} + (1-\hat{p})/w}$ 还原真实自然概率。”
3. **指标矩阵（Watched Metrics）**：
   - “**离线阶段**：我们首要关注 **GAUC（按 User 分组）**，消除用户活跃度偏差；同时通过 **PCOC** 和分桶可靠性曲线监控概率绝对校准度；针对新用户与长尾内容监控专项 **Slice GAUC**；”
   - “**线上阶段**：通过分层正交 A/B 实验追踪核心业务北极星指标（人均时长、DAU、次周留存）以及 P99 延迟护栏。”
4. **采样对指标的本质影响（Metric Shift Insight）**：
   “负样本下采样会直接拉高训练集观测 CTR 并改变数据先验分布。如果不做校准，**LogLoss 将无法跨实验横向对比，且输出的预估值无法直接用于下游多目标公式融分与 $eCPM$ 广告竞价**；而 GAUC 由于只依赖单用户内部候选样本得分的相对排名（Rank Order），在负样本随机下采样下具备优良的单调不变性。”
"""

with open("notes/BusinessAlgorithm/BusinessAlgorithm09 Industrial Recommendation Ranking Metrics Sampling.md", "w", encoding="utf-8") as f:
    f.write(zh_content)
print("Successfully generated Chinese note")
